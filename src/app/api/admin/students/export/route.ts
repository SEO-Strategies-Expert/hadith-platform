import { prisma } from "@/lib/prisma"; import { requireAdmin } from "@/lib/guard";
export async function GET(){await requireAdmin();const rows=await prisma.user.findMany({where:{role:"STUDENT"},orderBy:{name:"asc"}});const cell=(x:unknown)=>`"${String(x??"").replaceAll('"','""')}"`;const data=[["name","email","studentNo","program","status"],...rows.map(x=>[x.name,x.email,x.studentNo,x.program,x.status])].map(r=>r.map(cell).join(",")).join("\n");return new Response("\uFEFF"+data,{headers:{"content-type":"text/csv; charset=utf-8","content-disposition":"attachment; filename=students.csv"}})}
