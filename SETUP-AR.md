# منصّة الكلّية العليا للحديث النبوي — لوحة التحكم (Next.js)

هذا تطبيق **Next.js 16** يحوي لوحة التحكم (والموقع الديناميكي لاحقًا).
البنية: Next.js (App Router) + Vercel Postgres + Vercel Blob + NextAuth (مدير/محرّر) + Prisma.

## ✅ ما أُنجز (المرحلة 1)
- هيكل لوحة التحكم: قائمة جانبية بالأقسام + شريط علوي + صفحة قيادة ببطاقات.
- تسجيل دخول بالبريد/كلمة المرور، وحماية مسارات `/admin` (تحويل غير المصرّح لهم إلى `/login`).
- أدوار: **مدير (ADMIN)** و**محرّر (EDITOR)**.
- مخطط قاعدة بيانات أوّلي (جدول المستخدمين).
- كل شاشات الأقسام موجودة كصفحات مبدئية جاهزة للبناء في المرحلة القادمة.

## 🔑 ما تحتاج فعله لتفعيل اللوحة (مرّة واحدة)

### 1) جهّز قاعدة البيانات (Vercel Postgres)
- من لوحة Vercel: **Storage → Create Database → Postgres**.
- انسخ رابطي الاتصال: `DATABASE_URL` (المجمّع) و`DIRECT_URL` (المباشر/unpooled).

### 2) جهّز تخزين الصور (Vercel Blob)
- من Vercel: **Storage → Create → Blob**، وانسخ `BLOB_READ_WRITE_TOKEN`.

### 3) ولّد سرّ المصادقة
```bash
openssl rand -base64 32
```
ضع الناتج في `AUTH_SECRET`.

### 4) املأ متغيّرات البيئة
انسخ `.env.example` إلى `.env.local` (للتطوير المحلي) واملأ القيم. وفي Vercel أضِفها من
**Project → Settings → Environment Variables** (نفس المفاتيح).

### 5) أنشئ الجداول وأوّل مدير
```bash
npm install
npm run db:push        # ينشئ الجداول في قاعدة البيانات
npm run seed           # ينشئ حساب المدير من ADMIN_EMAIL/ADMIN_PASSWORD
```

### 6) شغّل محليًّا
```bash
npm run dev
# افتح http://localhost:3000/login
```

## 🚀 النشر على Vercel
- مشروع Vercel منفصل عن الموقع الحالي (نربطه عند اكتمال الصفحات الديناميكية).
- تأكّد من إضافة كل متغيّرات البيئة، ثم Deploy.

## 🗺️ المراحل القادمة
2. تصميم قاعدة البيانات الكاملة + ترحيل محتوى الموقع الحالي (عربي/إنجليزي) إليها.
3. جعل صفحات الموقع العامة تُقرأ من قاعدة البيانات (بنفس التصميم) ثم التحويل النهائي على نفس الرابط.
4. لوحة تحكم CRUD تفصيلية لكل شيء (نصوص، صور، أيقونات، روابط، تنقّل، هيئة، برامج، أخبار، ديوان، إصدارات، مكتبة، إعدادات، SEO، مستخدمون/أدوار).
