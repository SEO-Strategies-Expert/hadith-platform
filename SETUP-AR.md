# منصّة الكلّية العليا للحديث النبوي — لوحة التحكم + الموقع الديناميكي

تطبيق **Next.js 16** واحد يضمّ: الموقع العام (يُقرأ من قاعدة البيانات، عربي/إنجليزي، نفس
تصميم الموقع الأصلي حرفيًّا) + لوحة تحكم كاملة (CRUD) لكل المحتوى.
البنية: Next.js (App Router) + Vercel Postgres (Neon) + Vercel Blob + NextAuth (مدير/محرّر) + Prisma 6.

## ✅ الحالة: المراحل الأربع مكتملة ومُختبَرة
1. **الأساس:** لوحة تحكم (قائمة جانبية + شريط علوي)، تسجيل دخول، حماية `/admin`، أدوار مدير/محرّر.
2. **قاعدة البيانات:** مخطط كامل ثنائي اللغة + سكربت ترحيل يستورد كل محتوى الموقع الأصلي.
3. **الموقع العام ديناميكي:** كل صفحة تُقرأ من قاعدة البيانات (SSR) بنفس الروابط والتصميم تمامًا
   (`/`, `/about.html`, `/en`, `/en/about.html`...)، بما في ذلك قائمة "ديوان العلماء" في الرئيسية.
4. **لوحة CRUD كاملة:** كل قسم (صفحات، هيئة علمية، برامج، مقرّرات، أخبار، فعاليات، ديوان،
   إصدارات، أبحاث، مكتبة، تنقّل، سوشيال، شرائح هيرو، إعدادات، SEO، وسائط، مستخدمون) له نموذج
   إضافة/تعديل/حذف فعلي، مع أداة رفع صور مباشرة (بدل كتابة روابط يدويًا).
5. **ربط أقسام الواجهة بالجداول:** ما يُدخَل في اللوحة يظهر في الصفحة المقابلة مباشرةً
   (تفاصيل الخريطة أدناه).

## 🔗 خريطة الربط: أين يظهر كل قسم من اللوحة؟

| قسم اللوحة | الجدول | مكان الظهور في الموقع |
|---|---|---|
| شرائح الهيرو | `HeroSlide` | عارض الشرائح في الرئيسية (عربي/إنجليزي، صورة لكل لغة) |
| الهيئة العلمية | `Scholar` | شريط «نُخبة من كبار العلماء» في الرئيسية + `faculty.html` + `advisory-board.html` |
| البرامج (المراحل) | `ProgramStage` | مسار «المسار الأكاديمي» في الرئيسية (العنوان، البيان، قائمة المقرّرات، رابط المرحلة) |
| المقرّرات | `Course` | المقرّر **المرتبط بمرحلة** يخدم خطّة المرحلة؛ والمقرّر **بلا مرحلة** يظهر بطاقةً في `courses.html` |
| الأخبار | `NewsItem` | الخبر الرئيس + قائمة الأخبار في الرئيسية، وصدر `news.html` وشبكتها، وصفحة الخبر `news-detail.html?item=…` |
| الفعاليات | `Event` | تقويم «الفعاليات القادمة» في الرئيسية + بطاقات «اللقاءات القادمة» في `diwan.html` |
| ديوان العلماء | `DiwanThread` | قائمة المجالس في الرئيسية (مع التصفية والبحث) |
| أعداد المجلة | `JournalIssue` | بطاقات «أحدث الإصدارات» في `publications.html` |
| الأبحاث المحكّمة | `Paper` | فهرس `published-research.html` |
| المكتبة والمصادر | `LibraryResource` | `library.html` و`hadith-research-sites.html` |
| التنقّل / السوشيال / الإعدادات | `NavLink` / `SocialLink` / `Setting` | الهيدر والفوتر والشريط الإخباري في كل الصفحات |
| الصفحات و SEO | `Page` | نصّ الصفحة نفسها + العنوان والوصف وصورة المشاركة |

**قاعدة أمان مهمّة:** إذا كان الجدول فارغًا، تبقى البطاقات الأصلية كما هي بدل أن يفرغ القسم.
أي: لا يمكن لخطأ في الإدخال أن يترك قسمًا أبيض في الموقع.

## 🔴 عنصر واحد متبقٍّ يحتاج إجراء منك على Vercel

**مخزن Vercel Blob الحالي مضبوط "خاص" (Private) وليس "عام" (Public).**
معناه: رفع الصور من اللوحة يعمل تقنيًّا، لكن الصورة المرفوعة **لن تظهر للزوّار** (رابطها يرجع 403).
هذا لا يؤثّر على الصور الأصلية للموقع (باقية كما هي وتعمل)، فقط على أي صورة جديدة تُرفع من اللوحة.

**الإصلاح (على Vercel Dashboard، دقيقتان):**
1. **Storage** → افتح مخزن الـBlob الحالي → دوّر على إعداد الوصول (Access) وحوّله لـ **Public** إن وُجد الخيار.
2. لو ما فيش خيار تحويل، **أنشئ مخزن Blob جديد** واختر **Public** صراحةً عند الإنشاء.
3. لو أُنشئ مخزن جديد: انسخ `BLOB_READ_WRITE_TOKEN` الجديد وحدِّثه في **Settings → Environment Variables**، ثم **Redeploy**.

بعد الإصلاح، جرّب رفع صورة من `/admin/media` أو من أي نموذج فيه "رفع صورة" — هتظهر فورًا.

## 🔑 بيانات الدخول الحالية
```
الرابط: (رابط النشر على Vercel — راجع Deployments)
البريد: admin@hadith-faculty.com
كلمة المرور: (أُرسلت في المحادثة — غيّرها من /admin/users فور أول دخول)
```

## ⚙️ متغيّرات البيئة (مُفعَّلة بالفعل على Vercel)
| المتغيّر | المصدر |
|---|---|
| `POSTGRES_PRISMA_URL` / `POSTGRES_URL_NON_POOLING` | Vercel Postgres (Neon) — أُضيفت تلقائيًّا عند الربط |
| `BLOB_READ_WRITE_TOKEN` | Vercel Blob — حدِّثه إذا أُنشئ مخزن جديد (انظر أعلاه) |
| `AUTH_SECRET` | أُضيف يدويًّا في Environment Variables |

## 🖥️ التشغيل محليًّا (للتطوير فقط)
انسخ `.env.example` إلى `.env` واملأ نفس القيم أعلاه، ثم:
```bash
npm install
npm run dev
# افتح http://localhost:3000/login  (الموقع العام على /)
```
لإعادة تشغيل الترحيل من الصفر على قاعدة تطوير محلية:
```bash
npm run db:setup   # = db:push + seed + import
```

## 🚦 القرار المتبقّي — نقل الرابط الحيّ (Cutover)
هذا المشروع منشور حاليًّا على رابط Vercel الداخلي لفريقكم (وليس `hadith-faculty.vercel.app` —
ذاك الرابط لا يزال يشير إلى **الموقع الثابت القديم**). لجعل هذه المنصّة هي الموقع الرسمي فعليًّا:

1. من مشروع Vercel لهذا التطبيق (`hadith-platform`) → **Settings → Domains** → أضِف
   `hadith-faculty.vercel.app` (أو النطاق المخصّص إن وُجد).
2. Vercel سيطلب إزالته من المشروع القديم أولًا (نفس النطاق لا يمكن أن يكون بمشروعين).
3. بمجرد النقل، الرابط الرسمي سيعرض هذه المنصّة الديناميكية مباشرةً — بلا تغيير في الروابط
   الداخلية أو الخارجية (نفس البنية تمامًا).

**هذا إجراء على نطاق حيّ ومشترك — نفّذه فريقكم بأنفسهم من لوحة Vercel، أو أخبروني وقت التنفيذ
لأراجع النتيجة معكم مباشرة.**

## 📁 خريطة الكود السريعة
- `src/app/(site)/` — الموقع العام (طبقة جذر منفصلة، RTL/LTR ديناميكي).
- `src/app/(admin)/` — لوحة التحكم + تسجيل الدخول + API المصادقة.
- `src/components/site/` — الهيدر/الفوتر/الشريط الإخباري/عارض الصفحات (كلّها تقرأ من DB حيًّا).
- `src/lib/resources.ts` — تعريف كل مورد CRUD عام (news, library, threads...).
- `src/lib/site-sections.ts` — **محرّك ربط الأقسام**: يحلّل HTML الصفحة المخزَّن ويستبدل
  الحاويات المعروفة (`#heroStage`, `#facultyRail`, `ol.track`, `.news-grid`, `.agenda-list`,
  `#news-list`, `.profile-grid`, `#library-grid`, `#site-grid`, `#research-list`,
  `#courseGrid`, `#diwan-events`) بمحتوًى مُصيَّر من الجداول، ويطبّع الروابط النسبية.
- `src/lib/site-format.ts` — تنسيق التواريخ والأرقام العربية ومسارات الوسائط.
- `prisma/schema.prisma` — المخطط الكامل. `prisma/import.mjs` — سكربت الترحيل الأصلي.
- `src/proxy.ts` — حماية `/admin` + حقن لغة/مسار الموقع العام (بديل middleware.ts في Next 16).
