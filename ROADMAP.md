# خارطة الطريق — ما تبقّى من المنصّة

آخر تحديث: 2026-08-15

هذا الملفّ ينسّق العمل بين أكثر من جلسة تشتغل على المستودع في وقت واحد.
**قبل أي عمل: `git status`** — ثم اعمل داخل ملفّات مسارك فقط.

---

## أين نحن من العقد

العقد يلزمنا بثلاثة مكوّنات: **الموقع** + **تطبيق الجوال** + **المساعد الذكي**.

| المكوّن | الحالة |
|---|---|
| الموقع — التعريف والأخبار والمحاضرون والتواصل وSEO والاستجابة | ✅ منجز |
| الموقع — نظام التعلّم (مقرّرات/محاضرات/طالب/اختبارات/مدفوعات) | ❌ **هذا ما تبقّى** |
| تطبيق الجوال (iOS + Android) | ⏸️ مؤجَّل بقرار — بعد اكتمال الموقع |
| المساعد الذكي (Smart AI Bot) | ⏸️ بند منفصل، يبدأ باعتماد كتابي من العميل |

قرارات مثبَّتة:
- **البثّ: Zoom بحساب مدفوع.** الحساب الموثَّق يُضاف في آخر المشروع؛ التكامل
  مبنيّ ليعمل قبله وبعده بلا تغيير كود. التفاصيل في «تكامل Zoom» أدناه.
- **Calendly:** موضعها الوحيد الصحيح «المقابلة العلمية» في خطوات القبول
  (لقاء فردي). ليست أداة جدولة محاضرات ولا بثّ.
- **استضافة الفيديو المسجَّل:** تسجيلات المجالس تبقى على سحابة Zoom (مشمولة
  بالباقة) فلا تحتاج استضافة. أمّا دروس المقرّرات المُعدَّة مسبقًا فيُؤجَّل قرار
  مزوّدها حتى تُرفع دروس فعليّة؛ الحقول جاهزة لأيّ مزوّد.

---

## تكامل Zoom

**الحالة:** ✅ طبقة API جاهزة ومُختبَرة — [`src/lib/zoom.ts`](src/lib/zoom.ts)

تعمل في وضعين بلا تغيير كود:

| | بلا مفاتيح بيئة (الآن) | بالمفاتيح (بعد الحساب الموثَّق) |
|---|---|---|
| إنشاء المجلس | يدويّ: يُلصق الرابط في `joinUrl` | تلقائيّ عبر API |
| التسجيل | يدويّ | تلقائيّ من السحابة عبر الويب هوك |
| الحضور | يدويّ | تلقائيّ من تقرير المشاركين |

`isZoomConfigured()` هي المفتاح: ترجع `false` فيسقط النظام للوضع اليدوي بلا عطل.

**متغيّرات البيئة** (تُضاف إلى `.env.example` و`.env.local` وVercel عند الدمج):

```
ZOOM_ACCOUNT_ID=""            # من تطبيق Server-to-Server OAuth
ZOOM_CLIENT_ID=""
ZOOM_CLIENT_SECRET=""
ZOOM_WEBHOOK_SECRET_TOKEN=""  # من تبويب Feature في التطبيق نفسه
ZOOM_TIMEZONE="Asia/Qatar"    # اختياري
```

**تطبيقا Zoom المطلوبان** (من `marketplace.zoom.us` → Develop → Build App):

1. **Server-to-Server OAuth** — للجدولة والتسجيل والحضور. مطلوب الآن.
   الصلاحيات: `meeting:write:admin`, `meeting:read:admin`,
   `recording:read:admin`, `report:read:admin`.
2. **Meeting SDK** — فقط إن أردنا تضمين المجلس **داخل صفحة المنصّة** (وهو نصّ
   نطاق العمل: «بثّ مباشر داخل المنصّة»). مفاتيحه منفصلة تمامًا. **قرار مؤجَّل** —
   الانضمام يعمل عبر `joinUrl` في كلتا الحالتين.

**ملاحظات تشغيليّة:**
- `zoomStartUrl` هو رابط المضيف — **سرّي**، لا يُعرض إلا للمحاضر والمدير.
- تسجيلات Zoom محميّة بكلمة مرور؛ لذا `recordingPasscode` يُحفظ مع الرابط
  وإلّا تعذّرت المشاهدة.
- الويب هوك يتحقّق من توقيع Zoom ويرفض إعادة الإرسال بعد 5 دقائق.
  **يجب** تمرير الجسم الخامّ قبل `JSON.parse` وإلّا فشل التطابق.
- مسار الويب هوك ✅ [`src/app/api/webhooks/zoom/route.ts`](src/app/api/webhooks/zoom/route.ts) —
  يملأ `recordingUrl` عند `recording.completed`، ويزامن `Attendance` عند
  `meeting.ended`. سجّل في Zoom: `https://<النطاق>/api/webhooks/zoom`.
- المزامنة **لا تدهس** سجلّ حضور يدويًّا (`source='manual'`) — لأنّ المحاضر قد
  يكون صحّح الحضور بنفسه.

---

## المسارات

| # | المسار | يعتمد على | يملك هذه الملفّات | الحالة |
|---|---|---|---|---|
| ١ | الهوية وبوابة الطالب | — | `auth*`, `lib/guard.ts`, `proxy.ts`, `(site)/student*`, `components/site/StudentLogin*` | 🔴 جارٍ |
| ٠ | **مخطّط LMS دفعةً واحدة** | — | `prisma/schema.prisma` | ✅ مدموج (44 جدولًا) |
| ٢أ | نواة المقرّرات — طبقة الاستعلام | ٠ | `lib/lms.ts` | ✅ |
| ٢ب | بوابة الطالب: مقرّراتي/الدرس/التقدّم | ٢أ | `(site)/student/**`, `components/site/Student*` | ✅ (لم يُختبَر في متصفّح بعد) |
| ٢ج | اللوحة: بنية المقرّر + المجالس + التسجيل | ٢أ | `admin/courses|sessions|enrollments/**`, `resources.ts`, `admin-nav.ts` | 🔴 جارٍ |
| ٣ | تنبيهات المجالس بالبريد | ٠ | `lib/mailer.ts`, `email-templates.ts`, `reminders.ts`, `api/cron/**`, `vercel.json` | 🔴 جارٍ |
| ٤ | Calendly للمقابلة العلميّة | ٠ | `lib/calendly.ts`, `api/webhooks/calendly/**`, `interview.html`, `admin/settings/**` | 🔴 جارٍ |
| ٥ | لوحة الأكاديميين | ٢ب + ٢ج | `(instructor)/**` | ⬜ |
| ٦ | الواجبات والاختبارات | ٢ج | `admin/quizzes/**`, `lib/quiz.ts` | ⬜ |
| ٧ | الشهادات والإجازات + التحقّق العلني | ٢ج | `lib/certificates.ts`, صفحة `certificates` | ⬜ |
| ٨ | الرسوم والمدفوعات | ٢ج | `admin/payments/**` | ⬜ |

المسار ٨ حرّ تمامًا (ملفّات جديدة، صفر تعارض).
المسار ٧ يبدأ من رأس مال جاهز: `AdmissionApplication` مبنيّ ويعمل.

---

## ⚠️ ثغرة صلاحيّات أُصلحت (2026-08-15)

`src/proxy.ts` كان يحمي بوابة الطالب **بمطابقة تامّة** (`pathname === "/student"`)،
فالمسارات الفرعيّة `/student/course/*` و`/student/lesson/*` كانت خارج الحارس.
صار المطابقة بالبادئة — مع استثناء مقصود لـ`/student-login.html` لأنّه يبدأ
بالبادئة نفسها، ولو شُمل لدارت إعادة التوجيه بلا نهاية (فُحصت ١٠ حالات).

الحارس الأوسط **ليس حدّ الأمان الوحيد**: كل صفحة طالب وكل server action تُعيد
التحقّق بنفسها (`isEnrolled` + فحص `freePreview`/`visible`) — لأنّ server action
نقطة دخول شبكيّة مستقلّة لا يمرّ عبرها الحارس.

---

## تذكير المجالس على باقة Vercel المجّانيّة

باقة Hobby تسمح بمهمّة cron **يوميّة واحدة**، وأي جدولة أكثر تواترًا **تُفشل
النشر** (مؤكَّد من توثيق Vercel). فالتشغيل الساعيّ نُقل إلى **GitHub Actions**
— مجّانيّ، والمستودع عليه أصلًا: [`.github/workflows/lecture-reminders.yml`](.github/workflows/lecture-reminders.yml).

| | ينادي المسار | يغطّي |
|---|---|---|
| GitHub Actions | كل ساعة | تذكير الـ٢٤ ساعة **وتذكير الساعة** |
| `vercel.json` | يوميًّا ٣ فجرًا | شبكة أمان: تذكير الـ٢٤ ساعة لو تعطّل الأوّل |

**ما يضبطه العميل مرّةً واحدة** (GitHub → Settings → Secrets and variables → Actions):
- Secret `CRON_SECRET` — نفس قيمته في متغيّرات بيئة Vercel بالحرف.
- Variable `SITE_URL` — رابط الموقع بلا شرطة في آخره.

قبل ضبطهما تتخطّى المهمّة نفسها وتنتهي بنجاح، فلا تصل رسائل فشل كل ساعة.
عند الترقية إلى Pro يمكن نقل الجدولة الساعيّة إلى `vercel.json` والاستغناء عن
هذا الملفّ — لكنّه يعمل على الباقتين فلا ضرورة.

---

## قاعدة تفادي التعارض

عنق الزجاجة ملفّان مشتركان: **`prisma/schema.prisma`** و**`src/lib/resources.ts`**.

1. **المخطّط يُدمج مرّةً واحدة** (المسار ٠) — بعدها لا يعدّله أحد إلا بإعلان.
2. `resources.ts` يملكها **مسار واحد فقط** في كل لحظة؛ من يحتاج قسمًا إداريًّا
   جديدًا أثناء انشغالها يبني صفحة مخصّصة بدل التسجيل في المحرّك العام.
3. ما عدا ذلك: كل مسار داخل مجلّده. لا تعدّل ملفًّا يملكه مسار آخر.

---

## المسار ٠ — تمّ (2026-08-15)

المخطّط صار **44 جدولًا** (25 سابقًا + 19 جديدًا) + 6 enums جديدة.
أُضيفت كذلك 6 أعمدة على `Course` وعلاقات عكسيّة على `User`/`ProgramStage`/`Scholar`.

**التحقّق المُجرى قبل الاعتماد:**
- `prisma validate` → valid · `prisma format` → بلا فقدان سطر واحد (قُورن
  المحذوف بالمضاف بعد تطبيع المسافات: صفر).
- **محاكاة الإنتاج:** قاعدة Postgres محليّة بالمخطّط القديم + بيانات، ثم دُفع
  المخطّط الجديد فوقها → البيانات سليمة، 19 جدولًا أُنشئت، بلا تحذير فقدان.
- `tsc --noEmit` → صفر أخطاء.

✅ **دُفع إلى قاعدة الإنتاج (Neon)** — بموافقة صريحة، وبلا `--accept-data-loss`.
جرد قبل/بعد لكل جدول: **25 جدولًا/205 صفوف → 44 جدولًا/205 صفوف**؛ صفر تغيّر
في أي جدول قديم، صفر جدول مفقود، والجداول الجديدة فارغة كما يجب.
ثم `next build` نجح، و16 صفحة (عربي/إنجليزي/دخول/لوحة) رجعت 200 مقابل الإنتاج
بلا خطأ console أو خادم.

⚠️ **لا تشغّل `npm run import`** — يمسح جداول المحتوى ويُفقد كل ما حرّره العميل.

---

## بنود عالقة على العميل (لا حلّ برمجيّ لها)

- **حساب Zoom المدفوع + تطبيق Server-to-Server OAuth** — الكود جاهز وينتظر
  المفاتيح الأربعة. حتى ذلك الحين يعمل النظام بالوضع اليدوي بلا عطل.
- **١٨ رابطًا معطّلًا `href="#"`** في كل صفحة: ١٦ أيقونة تواصل + «البثّ المباشر»
  + «صفحات الجامعة». تُملأ من اللوحة (`SocialLink.url` والإعدادات).
- **حسابات المتاجر وبوّابة الدفع** — مطلوبة للمسار ٧ وللتطبيق (البند التاسع).
- **مواد الدراسة** (نصوص ودروس فيديو) — بلا محتوى فعليّ يبقى المسار ٢ فارغًا.

> مخزن Vercel Blob الخاصّ لم يعد عائقًا: `src/lib/blob.ts` يرفع بوصول خاصّ
> ويقدّم الملفّ عبر مسار من الموقع، ويعود للوصول العامّ تلقائيًّا لو حُوِّل المخزن.

---

## خارج نطاق العقد — يحتاج قرارًا

**منتدى الطلاب.** العقد لا يذكره، لكنّ الموقع يَعِد به في موضعين ظاهرين:
بطاقة «منتدى الطلاب» المميَّزة في منطقة الطالب، والخطوة الرابعة من القبول.
جدولاه في المسوّدة (القسم ٩) — إمّا يُحذفان، أو يُسعَّر ملحقًا وفق البند العاشر.
